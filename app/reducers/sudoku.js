

const defaultState = {
  // test: ''
}

// none, loading, ready, failed

export default function sudoku(state = defaultState, action) {
  switch (action.type) {
  // case 'testing':
  //     return state
  default:
    return state;
  }
}
